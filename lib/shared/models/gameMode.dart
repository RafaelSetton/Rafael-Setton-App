class GameMode {
  static const String add = "Adicao";
  static const String sub = "Subtracao";
  static const String mult = "Multiplicacao";
  static const String div = "Divisao";
  static const String addMult = add + " + " + mult;
  static const String addSub = add + " + " + sub;
  static const String addDiv = add + " + " + div;
  static const String subMult = sub + " + " + mult;
  static const String subDiv = sub + " + " + div;
  static const String multDiv = mult + " + " + div;
}
